# Internal authoring aids — never publish (standard is enforced via CI, not shipped)
/.claude/
/CLAUDE.md

# Dependencies & build
/vendor/
/composer.lock

# Tooling caches
.phpunit.result.cache
.php-cs-fixer.cache
/.phpunit.cache/

# IDE
/.idea/
/.vscode/
